apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  labels:
    cape-test-case: contradiction-neg-003
    owner-team: team-epsilon
  name: readonly-fs-clean-contradiction-neg-003
spec:
  background: false
  rules:
  - match:
      any:
      - resources:
          kinds:
          - Pod
          namespaces:
          - contra-ns-contradiction-neg-003
    name: check-readonly-rootfs
    validate:
      message: readOnlyRootFilesystem must be true
      pattern:
        spec:
          containers:
          - securityContext:
              readOnlyRootFilesystem: true
  validationFailureAction: Enforce
---
apiVersion: v1
kind: Pod
metadata:
  labels:
    cape-test-case: contradiction-neg-003
    owner-team: team-beta
  name: workload-clean-contradiction-neg-003
  namespace: contra-ns-contradiction-neg-003
spec:
  containers:
  - command:
    - sh
    - -c
    - echo test > /tmp/scratch/file.txt && sleep 3600
    image: docker.io/busybox:latest
    name: app
    securityContext:
      readOnlyRootFilesystem: true
    volumeMounts:
    - mountPath: /tmp/scratch
      name: scratch
  volumes:
  - emptyDir: {}
    name: scratch
